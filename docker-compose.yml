version: '3'

services:
  node:
    env_file:
      - .env
    image: "node:18"
    container_name: data_remar_api
    working_dir: /index
    volumes:
      - .:/index
    ports:
      - "3000:3000"
    command: "npm start"
    depends_on:
      - mongo

  mongo:
    env_file:
      - .env
    image: mongo

    restart: always
